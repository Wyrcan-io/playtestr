# Sprint 14 / R6-K release kit — 25 September 2026

Status: **kit completed 25 September; published and publicly verified by R6-V on 26 September 2026**.

## Reproducible stories

[Frozen release stories 36108031093](https://github.com/Wyrcan-io/playtestr/actions/runs/36108031093)
passed on native Windows amd64, Linux amd64, and macOS arm64 using only the
qualified executable hashes. Each host completed hero pass/defect/recovery,
stateful pass/cancel/defect/recovery, and compatibility pass/defect/recovery.
Every report confirmed cleanup. Exact runner, target, and executed spec hashes,
commands, postconditions, and disclosure are in
[`release/story-manifest.json`](../../release/story-manifest.json).

- Hero: a genuine wrong-selection build changes the external result from
  `Beta` to `Alpha`; the unchanged spec fails `unexpected_exit`, then recovers.
- Stateful: the adapter reads a real file; cancellation leaves it absent, the
  defect writes `port=8080` instead of `port=4242` and fails, then recovers.
- Compatibility: the defect reports a stale viewport after resize and fails
  `assertion_timeout`; successful runs independently write
  `size=80x20 modal=false`.

There was no editing or accelerated wait. Capture consists of full Actions
logs and Playtestr JSON/text artifacts. A headless Chrome screenshot of the
real frozen-Windows offline hero report is bundled with a static text fallback.
The [hero](../recipes/release-hero.md),
[stateful](../recipes/release-stateful.md), and
[compatibility](../recipes/release-compatibility.md) pages reproduce the path.
The hero page also attributes the real third-party Charm Gum v0.17.0 MIT recipe
and expressly does not imply maintainer endorsement.

## Watch-to-run and presentation checks

The path explains the bug, exact host archive names/checksums, prerequisites,
one command, real failure category/report, recovery, and adaptation to the Gum
recipe. The [static gallery](../demo-gallery.md) is readable without script or
video. [Compatibility](../qualified-compatibility-v0.4.0-rc.1.md) is scoped by
project/version/host/workflow and explains framework unit tests versus real-PTY
E2E tests.

Hugo 0.164.0 build/link validation passed: 35 HTML pages, 7,375 compressed
shared-code bytes and 11,550 compressed home bytes. The browser sweep passed 25
routes at 320, 375, 768, and 1440 px: one heading, no overflow after a gallery
image fix, skip link, keyboard Enter activation of mobile navigation,
meaningful control names, search, interactive evidence, reduced motion,
no-script transcript, nested routes/refresh, and 404. Mobile lab LCP was 344 ms
and CLS 0.000 in the recorded local run.

The offline report passed at 375 and 1440 px for keyboard skip navigation,
no-script content, body/diff contrast, long terminal/diff internal scrolling,
accessibility names, and **zero external requests**. Exported HTML is
self-contained. Automated accessibility-tree checks are not an actual screen
reader session. This headless environment did not provide an interactive
desktop/browser/screen-reader session, so that specific audit remains open and
no complete accessibility or universal WCAG claim is made.

## Kit inventory and audits

- [Release notes](../releases/v0.4.0-rc.1.md),
  [migration/update/downgrade/rollback](../migration-v0.4.0-rc.1.md),
  [evidence index](r6-evidence-index-2026-09-25.md), candidate manifest,
  checksums, supported scope, known limits, setup-action inspection/removal,
  installation/archive fallback, and three recipe pages.
- [Publication/post-publication commands](r6-publication-playbook-2026-09-25.md)
  and [channel drafts](r6-draft-announcements-2026-09-25.md) are ready but were
  not executed or sent.
- The original setup-action candidate at frozen source was later found
  incompatible with the aggregate checksum asset. Immutable repaired action
  revision `1c03904075512e67f53b0c94a13daa17f0383f1d` supports both checksum
  contracts and passed R6-V. Runner version and action commit remain separate
  pins; the published runner archives were not changed.
- License audit: archives contain the Apache-2.0 `LICENSE`, repository
  `THIRD_PARTY_NOTICES.md`, and README. `go.mod`/`go.sum` are frozen at the
  candidate source; notices cover shipped non-standard Go modules. Story
  fixtures are repository-owned; the linked Gum/bottom recipes identify their
  exact versions, upstream repositories, and MIT licenses.

The executable hashes in the candidate manifest are unchanged. Post-freeze
story, action and kit changes do not alter shipped runner bytes. At this R6-K
checkpoint no tag, GitHub release, release-asset upload, external deployment,
maintainer contact, announcement, or adoption work had occurred. Those are
subsequent facts recorded by the
[R6 publication and verification record](r6-publication-and-blocker-2026-09-25.md);
this historical checkpoint is not retroactively counted as publication.
