# Playtestr 0.2.0-alpha.2 readiness evidence

Date: 2026-08-19

Status: local Agent Advantage candidate; private, untagged, and unpublished.

## Implemented

- frozen five-game, ten-trial held-out terminal validation suite and tracked legacy baseline;
- World Model V2 with V1 migration, causal outcomes, reward components, frontiers, and prerequisite hypotheses;
- deterministic return-to-frontier planning and UCB-style adaptive role scheduling;
- cross-session campaign persistence for world, corpus, findings, routes, and agent learning;
- fresh-process completion/hidden route verification and minimization;
- strict repeated equal-action-budget Agent Advantage gate and CLI artifact;
- Replay V2 with V1 migration, backend/runtime identity, target manifest and artifact hashes, image reference, and resolved digest;
- real restrictive Docker PTY backend, normal cleanup, forced removal, and `docker-run` CLI.

## Agent Advantage evidence

Command: `node dist/cli.js advantage --suite fixtures/held-out/agent-advantage.v1.json --artifacts artifacts/agent-advantage/formal --trust-target`

- formal result: PASS;
- repeated full evaluations: 2;
- scenario/seed trials per evaluation: 10;
- intelligent mean evidence: `0.9418`;
- coverage-guided mean evidence: `0.4502`;
- margin: `+0.4916` (required `+0.10`);
- coverage comparison: 10 wins, 0 ties, 0 losses;
- scenario thresholds: 5/5 passed;
- action-budget parity: exact on every trial;
- cleanup failures: 0;
- semantic repeat determinism: true.

The pre-change implementation scored `0.4502`, tied coverage on all ten trials, and passed 0/5 frozen scenarios. The baseline, suite digest, and source boundary are recorded in `docs/evaluation/agent-advantage-held-out-v1-baseline.md`.

## Docker evidence

Local image: pre-existing `busybox:latest`; no pull was performed.

- normal interactive terminal target: passed, one action, cleanup confirmed;
- signal-resistant target: action-bounded run completed and forced cleanup confirmed;
- remaining matching containers after each run: none;
- Replay V2 backend: `docker-pty`;
- isolation capability: `container`;
- Docker server runtime version: `29.6.2`;
- resolved image ID: `sha256:dc2d74b28e4cf8984fa52af1f39bc7c3d9c73760b41a74d629f5d11b1ab28616`;
- final smoke target manifest hash: `20ea2393474c64bdb4f5eb3a865ce5bb8302ff04f4b98a94f3af978465984b48`;
- final smoke target artifact hash: `37bf54ea33ddc32d31cd2527a7b966e757a04a35e3031cc59daf68ccc8c59fcb`;
- no host mounts, no network, no pull, read-only root, all capabilities dropped, no-new-privileges, non-root user, memory/CPU/PID/tmpfs limits.

This proves the implemented lifecycle on the local Docker Desktop daemon. It does not make containers equivalent to virtual machines or prove every daemon/platform configuration safe.

## Local verification

- `npm run check`: 29 test files and 61 tests passed; typecheck and production build passed with PTY-heavy files serialized.
- Previously timing-sensitive hidden-route integration pair: 3/3 repeated two-file runs passed after raising the fixture settle window and preventing cross-file ConPTY contention.
- Original classified gauntlet: 12/12 discovery, robustness, and lifecycle scenarios passed with zero cleanup failures in 445 seconds.
- Lifecycle soak: 100/100 clean local PTY runs; remaining native handles: none.
- Production dependency audit: zero vulnerabilities.
- Packed package: 12 files, 157,599 bytes packed, 684,928 bytes unpacked.
- Clean temporary consumer: 74 public exports loaded; World Model, planner, advantage gate, route verifier, Docker backend, and Replay parser APIs present.
- Gamr adapter contract: 3/3 tests passed.
- Real Gamr bridge: three episodes, five actions, two states, `dispatch-briefing` milestone, Replay V2/local-pty identity, and clean lifecycle for every episode.

## Remaining external gates

- GitHub Actions must pass on the pushed commit across the configured OS/Node matrix.
- The repository owner must explicitly approve AGPL-3.0 obligations.
- npm trusted publishing/OIDC and provenance must be configured before removing `private: true`.
- Mutable Docker tags are weaker evidence than digest-pinned images; production examples should pin digests.
- Hosted model providers remain optional and must add redaction, cost, timeout, and deterministic fallback evidence before release claims.
- Browser/graphical execution remains deferred until its separate runtime and visual-baseline gate is implemented.
- No tag or npm publication is authorized by this document.
